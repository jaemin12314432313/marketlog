# VITE_API_BASE_URL: Base URL of the FastAPI backend (marketlog-backend07.25),
# called directly from the browser. The backend already allows all origins via CORS.
VITE_API_BASE_URL="http://localhost:8000"
