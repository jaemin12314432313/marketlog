"""mlv2 — 마켓로그 Vision AI, 품목 인식 파이프라인 재작성판 (2026-08-10).

기존 `marketlog_vision` 패키지와 **공존**한다. 지우지 않는 이유는 기존 체크포인트와
문서(HANDOFF_*)가 그 코드를 참조하고 있어서다. 새 작업은 전부 이쪽에서 한다.

설계 원칙 넷 (전부 기존 코드에서 실제로 데였던 것에 대한 대응이다):

1. **분할은 파일에 박아둔다.** 기존 코드는 `build_dataloaders()` 안에서 난수로
   train/val 을 갈랐다. 시드가 같아도 데이터가 한 장만 늘면 분할 전체가 바뀌고,
   "이 체크포인트가 어떤 개체로 평가됐는지" 를 사후에 확인할 방법이 없었다.
   → `manifest.py` 가 이미지 목록 + 개체 ID + split 을 CSV 로 고정한다.

2. **모델 판단은 스튜디오 val_acc 로 하지 않는다.** val_acc 는 epoch 4에 1.0000 으로
   포화한다(v3/v4 둘 다). 포화된 지표로 best 를 고르면 남은 epoch 은 스튜디오 도메인
   과적합에만 쓰인다 — v4 가 실사진 22/30 → 14/30 으로 무너진 원인이 이것이다.
   → `train.py` 는 매 epoch 실사진 세트를 같이 재고, `--select-by real` 이 기본이다.

3. **개체 단위 분할을 검증까지 한다.** 분할했다고 주장하는 것과 실제로 누수가 없는 것은
   다르다. → `manifest.verify_split()` 이 교집합을 세고, 0이 아니면 학습을 시작하지 않는다.

4. **로컬(3.13)과 서버(3.8.10) 양쪽에서 돈다.** 모든 모듈이
   `from __future__ import annotations` 로 시작하고, 런타임 제네릭(`dict[str, int]` 를
   실제로 호출하는 코드)을 쓰지 않는다.
"""
from __future__ import annotations

__version__ = "0.1.0"

ITEM_CLASSES = (
    "감", "감귤", "감자", "마늘", "무",
    "배", "배추", "사과", "양배추", "양파",
)
