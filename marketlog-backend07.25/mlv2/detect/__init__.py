"""검출(YOLO) 파이프라인 — 합성 데이터로 현장 적응.

**왜 검출인가.** 기존 파이프라인은 "이미지 한 장 = 품목 하나" 분류였다. 스튜디오
데이터가 개체 단독 촬영이라 그렇게 만들 수밖에 없었다. 그런데 실사용은 시장 매대이고,
거기엔 여러 품목이 겹쳐 널려 있다. 분류기 하나로는 못 다룬다.

**왜 합성인가.** 원본 라벨 JSON 의 `bndbox` 는 항상 이미지 전체다. 즉 이 데이터셋에는
쓸 수 있는 바운딩 박스가 **한 개도 없다.** 21만 장을 손으로 라벨링할 게 아니라면
합성이 유일한 길이다. (Cut-Paste-Learn, Simple Copy-Paste 계열의 표준 기법이다.)

**합성이 공짜로 주는 것: 크기 단서.**
원본은 모든 품목이 프레임을 꽉 채워 찍혀 있다 — 마늘도 배추도 화면에서 같은 크기다.
즉 스튜디오 데이터는 **크기 정보를 물리적으로 제거한 데이터**다. 기존 모델이
{감자↔마늘}(실측 7.4cm vs 3.7cm, 정확히 2배)을 10배 데이터로도 못 푼 이유가 여기 있을
가능성이 크다. 합성은 한 장면에 여러 품목을 실제 cm 비율로 놓으므로 이 단서가 복원된다.
크기 값은 `data/label_metadata.csv` 의 `width`/`weight` 컬럼에 21만 건 다 있다.

**측정해서 알아낸 제약 셋** (2026-08-10, 600장 표본):
  - 원본의 72% 가 프레임 밖으로 잘려 있다(테두리 접촉 5% 초과). 양파는 98% 가 잘렸다.
    → 누끼 뱅크는 원본 21만 장 **전체**에서 필터링해야 한다. v2(5만 장)로는 모자란다.
  - 라벨의 `truncated` 컬럼은 21만 건 전부 'on' 이라 쓸모없다. 픽셀로 직접 재야 한다.
  - 누끼를 RGB 거리로 뜨면 그림자가 전경으로 딸려온다. Lab 색도(a*,b*)로 뜨면 해결된다.
    무(흰 물체/흰 배경)에서 전경 비율 0.614 → 0.270 으로 갈렸다. matting.py 참조.

모듈 구성:
    matting.py     누끼 + 품질 점수
    cutouts.py     원본 zip → 누끼 뱅크(RGBA PNG + 인덱스 CSV)
    backgrounds.py 배경 수집 가이드·검증
    compose.py     합성 + YOLO 라벨 자동 생성
    train_yolo.py  Ultralytics YOLO11 학습 래퍼

⚠️ 라이선스: Ultralytics YOLO11 은 AGPL-3.0 이다. 해커톤·연구 범위에서는 문제없지만,
결과물을 상용 배포하면 소스 공개 의무가 생길 수 있다. 그래서 합성 데이터셋은 표준 YOLO
포맷으로 만들어 두었다 — 나중에 YOLOX(Apache-2.0) 등으로 모델만 갈아끼울 수 있다.
"""
from __future__ import annotations

# 품목별 실측 물리 크기(cm). data/label_metadata.csv 의 width 중앙값이다.
# 합성 시 이 비율로 배치해야 크기 단서가 학습 신호로 들어간다.
PHYSICAL_WIDTH_CM = {
    "배추": 18.0,
    "양배추": 16.3,
    "무": 10.6,
    "배": 9.9,
    "사과": 8.7,
    "양파": 8.7,
    "감": 7.7,
    "감귤": 7.4,
    "감자": 7.4,
    "마늘": 3.7,
}

# 검출 클래스는 1개다. 품목 판단은 뒤단 분류기가 한다.
# 이유: (1) 검출이 쉬워져 적은 데이터로 수렴하고 (2) 품목이 추가돼도 분류기만 재학습하면
# 되며 (3) 진혁님이 요청한 '분류기 현장적응'과 역할이 깔끔하게 갈린다.
DETECT_CLASSES = ["produce"]
