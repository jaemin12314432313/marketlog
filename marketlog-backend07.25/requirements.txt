fastapi==0.115.0
uvicorn==0.24.0
pydantic==2.13.4
python-multipart==0.0.6
python-dotenv==1.0.1
httpx==0.28.1
sqlalchemy==2.0.31
psycopg2-binary==2.9.10
bcrypt==4.1.3
pyjwt==2.9.0
email-validator==2.2.0
google-genai==2.16.0
openai>=1.50
google-cloud-storage==2.18.2

# marketlog_vision (품목인식/품질등급 모델) 및 AI 스캔에서 사용.
# consumer.py가 최상단에서 import하므로 빠지면 서버 자체가 부팅되지 않는다.
# GPU 없는 배포 환경이면 설치 전에 CPU 전용 휠 인덱스를 추가로 지정할 것:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
torchvision==0.28.0
timm==1.0.28
Pillow==12.1.0

# mlv2.pipeline (새 비전 파이프라인, 2026-08-13 교체)의 필수 의존 — YOLO 검출기.
# 없으면 mlv2.pipeline import 자체가 실패한다.
ultralytics>=8.3